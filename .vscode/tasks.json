{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Git: Add/Commit/Push",
			"type": "shell",
			"command": "powershell",
			"args": [
				"-NoProfile",
				"-Command",
				"$msg = Read-Host 'Commit message'; if ([string]::IsNullOrWhiteSpace($msg)) { $msg = 'chore: sync'; }; npm run sync -- $msg"
			],
			"isBackground": false,
			"problemMatcher": [],
			"group": "none"
		}
	]
}